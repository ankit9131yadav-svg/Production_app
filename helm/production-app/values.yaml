namespace: production

replicaCount: 2

image:
  repository: ankit9131/production-app
  tag: latest

service:
  type: NodePort
  port: 80
  targetPort: 80

config:
  APP_ENV: production
  APP_NAME: Production DevOps Platform
  APP_PORT: "80"

secret:
  DB_USERNAME: admin
  DB_PASSWORD: password123
  API_KEY: devops-secret-key
